# This file contains most of the configuration variables that your app needs.
# When your app is initialized, the variables in config.py are used to configure Flask and its extensions are accessible via the app.config dictionary – e.g. app.config["DEBUG"].
DEBUG = True